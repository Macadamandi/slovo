import Content from '../content/Content';
import CreateNewSet from '../createNewSet/CreateNewSet';

const Sets = () => {
  return(
    <Content>
          <CreateNewSet/>
    </Content>
  );
}

export default Sets;